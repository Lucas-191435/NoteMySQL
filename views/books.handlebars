<h1>Livros disponiveis:</h1>

<div class="books-container">
  {{#each books}}
  <div>#{{this.id}} - 
    <span class="bold">
      <a href="/books/{{this.id}}">{{this.title}} - {{this.pageqty}} páginas</a>
    </span>
  </div>
  {{/each}}
</div>